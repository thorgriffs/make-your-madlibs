<div class="row" style="margin-top: 10%;">
  <div class="col-3"></div>
  <div class="col-9">
    <div class="card" style=" width: 30rem;">
      <div class="card-body">
        <a href="#">
          <h5 class="card-title">{{this.title}}</h5>
        </a>

        <p class="libDiscription">{{this.teaser}}</p>

        <form>
          {{#each blanks}}
          <div class="form-group row">
            <label for="blank-{{this.index}}" class="col-sm-6 col-form-label">
              {{this.variant}} {{this.category}}
            </label>
            <div class="col-sm-6">
              <input name="template-{{id}}" type="hidden">
              <input id="blank-{{@index}}" name="{{@index}}" type="text" class="form-control">
            </div>
          </div>
        </form>

        <button class="makeLib btn btn-primary" id="{{id}}">Make Lib</button>

      </div>
    </div>
  </div>
</div>